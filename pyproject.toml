[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "open-model-supply-chain-radar"
version = "0.1.0"
description = "Open-model release supply-chain readiness scoring"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["model_supply_chain_radar*"]
